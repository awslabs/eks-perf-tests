apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - tekton/namespace.yaml
  - tekton/storage-class.yaml
  - tekton/bucket.yaml
  - tekton/operator-install.yaml
  # RBAC, OIDC secret, and ingress are managed by CDK (domain-routing component)
